{
  "name": "scheduled-dispatch",
  "version": "1.0.0",
  "private": true,
  "description": "Cron Appwrite Function (Bun runtime) that pings the apps' secret-gated dispatch/sync endpoints.",
  "type": "module",
  "main": "src/main.ts",
  "scripts": {
    "check-types": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.6.0"
  }
}
