'use client';

/**
 * Products editor plugin set.
 * Base (already includes Table + Callout) — products lean heavily on tables
 * for spec sheets and callouts for feature highlights.
 * No extra plugins needed beyond base for now.
 */

export { BaseEditorPlugins as ProductsEditorPlugins } from './base';
