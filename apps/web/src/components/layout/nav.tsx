// The flat navigation was replaced by the mega-menu. This re-export keeps the
// legacy import path (`@/components/layout/nav`) working; the implementation now
// lives in `@/components/nav/mega-nav`.
export { Navigation } from "@/components/nav/mega-nav";
